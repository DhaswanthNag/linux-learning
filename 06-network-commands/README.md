# 🔹 Network Commands

Practical Linux command notes with screenshots. Replace the placeholder images in the `images/` folder with your own terminal screenshots.

## `ip addr`

Show network interfaces and IP addresses.

```bash
ip addr
```

![ip-addr screenshot](images/ip-addr.png)

## `ip route`

Show the routing table.

```bash
ip route
```

![ip-route screenshot](images/ip-route.png)

## `ping google.com`

Test network connectivity.

```bash
ping google.com
```

![ping screenshot](images/ping.png)

## `ss -tuln`

Display network sockets and listening ports.

```bash
ss -tuln
```

![ss screenshot](images/ss.png)

## `curl https://example.com`

Make HTTP/network requests.

```bash
curl https://example.com
```

![curl screenshot](images/curl.png)

## `wget https://example.com/file.zip`

Download files from the network.

```bash
wget https://example.com/file.zip
```

![wget screenshot](images/wget.png)

## `hostname`

Show the system hostname.

```bash
hostname
```

![hostname screenshot](images/hostname.png)

## `nslookup example.com`

Query DNS information.

```bash
nslookup example.com
```

![nslookup screenshot](images/nslookup.png)

## `dig example.com`

Perform DNS lookups (if installed).

```bash
dig example.com
```

![dig screenshot](images/dig.png)

## `traceroute example.com`

Trace the route to a host (if installed).

```bash
traceroute example.com
```

![traceroute screenshot](images/traceroute.png)

## `ssh user@server-ip`

Connect securely to a remote machine.

```bash
ssh user@server-ip
```

![ssh screenshot](images/ssh.png)

## `scp file.txt user@server:/home/user/`

Securely copy files over SSH.

```bash
scp file.txt user@server:/home/user/
```

![scp screenshot](images/scp.png)
